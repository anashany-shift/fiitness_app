import 'package:flutter/material.dart';

class GenderForm extends StatelessWidget {
  const GenderForm({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Gender Form'),
    );
  }
}