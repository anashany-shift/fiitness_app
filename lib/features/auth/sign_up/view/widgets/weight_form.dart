import 'package:flutter/material.dart';

class WeightForm extends StatelessWidget {
  const WeightForm({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Weight Form'),
    );
  }
  }