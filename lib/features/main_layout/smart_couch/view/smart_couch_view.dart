import 'package:flutter/material.dart';

class SmartCouchview extends StatelessWidget {
  const SmartCouchview({super.key});

  @override
  Widget build(BuildContext context) {
    return Column();
  }
}